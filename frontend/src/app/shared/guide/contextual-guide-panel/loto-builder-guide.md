1. file management:
    - use left menu to search and select existing file
    - use + button to add new file to the system
    - files menu has 2 views: toggle-menu and table
    - files data (name, description, type) can be edited by right clicking item
    - in toggle-menu view files are groupped by type - use tabs to navigate
2. loto point management
    - ways to add new loto points: 
        - in the left menu, select loto points
        - click + button
        - directly on the file - open file
        - right click and drag to draw shape
    ways to edit loto points: 
        -