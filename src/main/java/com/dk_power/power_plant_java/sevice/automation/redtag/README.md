# Red Tag LOTO Automation (rebuilt)

A clean, self-contained rebuild of the SikuliX desktop automation that builds
LOTO procedures in the **Red Tag Enterprise** application. It lives entirely in
this package and does **not** touch the legacy `sevice/automation/` code, which
stays in place as a reference until this version is verified on a real machine.

## Why a rebuild

The legacy `RedTagAutomationService` was a ~1,700-line class: ~200 loose
`Pattern` constants with hard-coded `J:/` paths, magic-number offsets, dead test
methods, and all four permit types tangled together. This version fixes that:

| Concern | Legacy | Here |
|---|---|---|
| Screenshots | ~200 `new Pattern("J:/...")` on a network drive | `RedTagPattern` enum + bundled images in the repo (`PatternCatalog`) |
| Screen API | inline `screen.find(...).offset(...).click()` | `SikuliDriver` — one wrapper, retries, uniform errors |
| Orchestration | mixed into the god class | `StepEngine` — generic, permit-agnostic |
| LOTO flow | semi-auto (app + login + builder opened by hand) | `LoginFlow` + `LotoBuildFlow` — fully automated |
| Failures | stringly-typed `"Failed ..."` | typed `AutomationException` |

## Layout

```
config/    RedTagAutomationProperties   externalised paths / timeouts / credentials
core/      RedTagPattern                enum catalogue of every screenshot
           PatternCatalog               resolves names -> SikuliX Patterns
           SikuliDriver                 thin wrapper over the SikuliX Screen API
           RedTagWindow                 launch / focus / maximise the Red Tag app
           AutomationException          single typed failure
session/   AutomationStep / AutomationSession / StepStatus / SessionStatus
           StepEngine                   runs a session: pause/resume/stop/retry/skip
flow/      LoginFlow                    open app + sign in
           LotoBuildFlow                open builder -> header -> points -> details
           SafeWorkBuildFlow            open SW form -> header -> hazard/permit/PPE grid -> save
           HotWorkBuildFlow             open HW form -> header/work type -> checklist ->
                                        air test -> requirements/approval -> save
           ConfinedSpaceBuildFlow       open CS form (Reclassified / Permit Required) -> save
progress/  RedTagProgressBroadcaster    SSE progress to the browser
RedTagLotoAutomationService             facade: turns a LotoDto into a session
RedTagSafeWorkAutomationService         facade: turns a SafeWorkDto into a session
```

Controller: `controller/automation/redtag/RedTagLotoAutomationController` at
`/ng/red-tag-automation/*` — `POST /loto/{id}/build` and `POST /safe-work/{id}/build`.

## Flow: build a LOTO

```
POST /ng/red-tag-automation/loto/{lotoId}/build
  -> RedTagLotoAutomationService.startLotoBuild(LotoDto)
       step 0  open-app      LoginFlow.ensureAppOpen()
       step 1  login         LoginFlow.ensureLoggedIn()
       step 2  open-builder  LotoBuildFlow.openLotoBuilder()
       step 3  fill-header   LotoBuildFlow.fillHeader(loto)
       step 4  add-points    LotoBuildFlow.addPoints(points)
       step 5  fill-details  LotoBuildFlow.openLotoDetails() + fillLotoDetails(loto)
       step 6  save          submitLotoDetails() + readPermitNumber() -> writes redTagNum
  -> StepEngine runs the steps on a background thread
  -> RedTagProgressBroadcaster streams progress over SSE
```

Controls: `POST /pause|/resume|/stop|/retry-step|/skip-step`,
`GET /status`, `GET /stream` (SSE), `POST /manual-confirmation?enabled=`.

## Patterns / screenshots

See `project/features/red-tag-automation/PATTERN_MANIFEST.md`. Images are bundled
at `src/main/resources/automation/redtag/patterns/`. The LOTO set is generated by
`generate-patterns.py`; the Safe Work and Hot Work **form** patterns come from
`import-form-patterns.ps1` + `form-patterns-2026-09-03.manifest`.

A SikuliX pattern matches only at the scale it was captured at, and a permit form
is a document view whose zoom follows the window, the DPI and the last operator's
Ctrl+wheel. Rather than recapture crops every time a display changes — which is
how the catalogue once ended up holding three different scales at once — the zoom
is **measured**: `SikuliDriver.calibrateScale` finds a section anchor across a
ladder of zooms when the form opens, `PatternCatalog` rescales every pattern in
`RedTagPattern.FORM_CONTENT` by the result, and offsets go through
`SikuliDriver.px`. Application chrome is not rescaled; it follows the OS widget
scale, not the form's.

## Scope

LOTO is automated **end to end** — launch, login, builder, header, points, the
post-Continue LOTO information form, submit, and OCR read-back of the new Red Tag
number (written to `loto.redTagNum`). The LOTO list is assumed ungrouped so the
newest LOTO is the top row.

Safe Work is automated end to end too — open the SW form, fill the header, the
hazard / permit / PPE checkbox grids, the footer, save, and OCR the permit number.
Every checkbox has its own label crop under `safe-work/labels/` and is clicked at
a fixed offset from the crop's left edge.

Two deliberate choices in the SW permits section:

- The **`#` boxes beside LOTO Required / Hot Work Permit / Confined Space are left
  empty**. Those numbers are written by the Associate flow, which links the permits
  Red Tag actually issued; typing the local description into the same box would put
  a second, unlinked number beside them.
- **Special Instructions carries the LOTO lock boxes** — `LOTO Boxes: 4, 7`,
  appended after whatever the issuer wrote. The box is what a worker walks up to,
  and nothing else on the tag says which one. The numbers come from the permit's
  daily package (`NgSafeWorkService.getLotoBoxNumbers`), resolved before the build
  starts because the engine's background thread cannot walk that lazy association.

Hot Work is automated end to end against the **2026-08-27 form revision**: header
and Work Type, the 9-row Y checklist, the Fire Protection System bars, Initial Air
Test, continuous air monitoring, Fire Watch duration, the two names, special
instructions and the issuer-approval line, then save and OCR. It deliberately
stops there. The Active Hot Work times and the whole cancellation/monitoring
section are filled by the fire watch as the job runs and closes out, not by the
issuer at permit-creation time.

Confined Space (Reclassified + Permit Required) follows the same `flow/` + facade
pattern. Its patterns predate the shared 2026-09-03 capture, so its flow calls
`driver.resetScale()` on open rather than calibrating.
