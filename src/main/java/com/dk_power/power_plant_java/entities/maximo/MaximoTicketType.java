package com.dk_power.power_plant_java.entities.maximo;

/** Which Maximo ticket a {@link MaximoTicketAsset} index row summarizes. */
public enum MaximoTicketType {
    SR,   // Service Request (mxapisr)
    WO    // Work Order (mxapiwodetail)
}
