package com.dk_power.power_plant_java.entities.maximo;

/** How often a recurring PM repeats. Inferred from occurrence spacing; operator can override. */
public enum RecurrenceCadence {
    DAY, WEEK, BIWEEK, MONTH, QUARTER, OTHER
}
